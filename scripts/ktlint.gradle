apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    version = "1.4.0"

    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "html"
    }

    outputColorName = "RED"
}